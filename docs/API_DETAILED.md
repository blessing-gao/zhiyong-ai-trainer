# AI 教育平台 - 前端 Admin 管理系统 API 接口文档

## 📋 文档概述

本文档列出了前端 Admin 管理系统中**题库管理**和**考试管理**模块所需的所有 API 接口。

---

## 🎯 一、题库管理 API 接口

### 1.1 获取题库列表
**接口名称**: 获取题库列表  
**请求方法**: `GET`  
**请求路径**: `/api/admin/question-banks`  
**请求参数**:
```json
{
  "page": 1,
  "pageSize": 10,
  "search": "搜索关键词",
  "category": "AI基础理论",
  "difficulty": "初级"
}
```
**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "list": [
      {
        "id": 1,
        "name": "人工智能概述",
        "category": "基础理论",
        "questions": 150,
        "difficulty": "初级",
        "usageCount": 156,
        "correctRate": 78,
        "createDate": "2024-01-15",
        "updateDate": "2024-03-20"
      }
    ]
  }
}
```

### 1.2 获取题库统计数据
**接口名称**: 获取题库统计  
**请求方法**: `GET`  
**请求路径**: `/api/admin/question-banks/statistics`  
**响应示例**:
```json
{
  "code": 200,
  "data": {
    "statistics": [
      {
        "category": "AI基础理论",
        "total": 458,
        "easy": 180,
        "medium": 205,
        "hard": 73
      }
    ]
  }
}
```

### 1.3 创建题库
**接口名称**: 创建题库  
**请求方法**: `POST`  
**请求路径**: `/api/admin/question-banks`  
**请求体**:
```json
{
  "name": "新题库名称",
  "category": "AI基础理论",
  "description": "题库描述",
  "difficulty": "初级"
}
```

### 1.4 编辑题库
**接口名称**: 编辑题库  
**请求方法**: `PUT`  
**请求路径**: `/api/admin/question-banks/{id}`  
**请求体**:
```json
{
  "name": "更新的题库名称",
  "category": "AI基础理论",
  "description": "更新的描述",
  "difficulty": "中级"
}
```

### 1.5 删除题库
**接口名称**: 删除题库  
**请求方法**: `DELETE`  
**请求路径**: `/api/admin/question-banks/{id}`

### 1.6 获取题库详情
**接口名称**: 获取题库详情  
**请求方法**: `GET`  
**请求路径**: `/api/admin/question-banks/{id}`

### 1.7 批量导入题目
**接口名称**: 批量导入题目  
**请求方法**: `POST`  
**请求路径**: `/api/admin/question-banks/import`  
**请求体**: FormData (文件上传)
```
file: 题库文件 (Excel/CSV)
bankId: 题库ID
```

### 1.8 导出题库
**接口名称**: 导出题库  
**请求方法**: `GET`  
**请求路径**: `/api/admin/question-banks/{id}/export`  
**响应**: 文件下载

### 1.9 获取题目列表
**接口名称**: 获取题目列表  
**请求方法**: `GET`  
**请求路径**: `/api/admin/questions`  
**请求参数**:
```json
{
  "page": 1,
  "pageSize": 10,
  "bankId": 1,
  "search": "搜索关键词",
  "category": "AI基础理论",
  "difficulty": "初级",
  "type": "选择题"
}
```
**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 150,
    "list": [
      {
        "id": 1,
        "question": "什么是机器学习？",
        "type": "选择题",
        "category": "AI基础理论",
        "difficulty": "初级",
        "options": ["A. 选项1", "B. 选项2", "C. 选项3", "D. 选项4"],
        "correctAnswer": 0,
        "analysis": "详细解析",
        "usageCount": 156,
        "correctRate": 78,
        "createDate": "2024-01-15"
      }
    ]
  }
}
```

### 1.10 创建题目
**接口名称**: 创建题目  
**请求方法**: `POST`  
**请求路径**: `/api/admin/questions`  
**请求体**:
```json
{
  "bankId": 1,
  "question": "题目内容",
  "type": "选择题",
  "category": "AI基础理论",
  "difficulty": "初级",
  "options": ["选项A", "选项B", "选项C", "选项D"],
  "correctAnswer": 0,
  "analysis": "详细解析"
}
```

### 1.11 编辑题目
**接口名称**: 编辑题目  
**请求方法**: `PUT`  
**请求路径**: `/api/admin/questions/{id}`

### 1.12 删除题目
**接口名称**: 删除题目  
**请求方法**: `DELETE`  
**请求路径**: `/api/admin/questions/{id}`

### 1.13 获取题目详情
**接口名称**: 获取题目详情  
**请求方法**: `GET`  
**请求路径**: `/api/admin/questions/{id}`

---

## 🎓 二、考试管理 API 接口

### 2.1 获取考试列表
**接口名称**: 获取考试列表  
**请求方法**: `GET`  
**请求路径**: `/api/admin/exams`  
**请求参数**:
```json
{
  "page": 1,
  "pageSize": 10,
  "search": "搜索关键词",
  "status": "进行中",
  "type": "正式考试"
}
```
**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 234,
    "list": [
      {
        "id": 1,
        "name": "AI训练师认证考试-2024年3月",
        "type": "正式考试",
        "date": "2024-03-25",
        "startTime": "09:00",
        "endTime": "11:00",
        "duration": 120,
        "participants": 85,
        "averageScore": 85.6,
        "passRate": 87,
        "status": "进行中",
        "questionCount": 100,
        "totalScore": 100,
        "createDate": "2024-03-20"
      }
    ]
  }
}
```

### 2.2 获取考试统计数据
**接口名称**: 获取考试统计  
**请求方法**: `GET`  
**请求路径**: `/api/admin/exams/statistics`  
**响应示例**:
```json
{
  "code": 200,
  "data": {
    "todayExams": 12,
    "weekExams": 45,
    "totalExams": 234,
    "averagePassRate": 87.5
  }
}
```

### 2.3 创建考试
**接口名称**: 创建考试  
**请求方法**: `POST`  
**请求路径**: `/api/admin/exams`  
**请求体**:
```json
{
  "name": "考试名称",
  "type": "正式考试",
  "date": "2024-04-15",
  "startTime": "09:00",
  "endTime": "11:00",
  "duration": 120,
  "totalScore": 100,
  "passingScore": 60,
  "description": "考试描述",
  "questionBankIds": [1, 2, 3],
  "questionCount": 100,
  "randomOrder": true,
  "showAnswers": false
}
```

### 2.4 编辑考试
**接口名称**: 编辑考试  
**请求方法**: `PUT`  
**请求路径**: `/api/admin/exams/{id}`

### 2.5 删除考试
**接口名称**: 删除考试  
**请求方法**: `DELETE`  
**请求路径**: `/api/admin/exams/{id}`

### 2.6 获取考试详情
**接口名称**: 获取考试详情  
**请求方法**: `GET`  
**请求路径**: `/api/admin/exams/{id}`

### 2.7 获取考试成绩列表
**接口名称**: 获取考试成绩  
**请求方法**: `GET`  
**请求路径**: `/api/admin/exams/{id}/scores`  
**请求参数**:
```json
{
  "page": 1,
  "pageSize": 10,
  "search": "学生名称"
}
```
**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 85,
    "list": [
      {
        "studentId": 1,
        "studentName": "张三",
        "score": 92,
        "status": "通过",
        "submitTime": "2024-03-25 10:30:00",
        "duration": 95,
        "correctCount": 92,
        "totalCount": 100
      }
    ]
  }
}
```

### 2.8 导出考试成绩
**接口名称**: 导出考试成绩  
**请求方法**: `GET`  
**请求路径**: `/api/admin/exams/{id}/export-scores`  
**响应**: 文件下载

### 2.9 获取考试监控数据
**接口名称**: 获取考试监控  
**请求方法**: `GET`  
**请求路径**: `/api/admin/exams/{id}/monitor`  
**响应示例**:
```json
{
  "code": 200,
  "data": {
    "totalParticipants": 85,
    "completedCount": 78,
    "inProgressCount": 5,
    "notStartedCount": 2,
    "averageProgress": 92,
    "averageScore": 85.6
  }
}
```

### 2.10 发布考试
**接口名称**: 发布考试  
**请求方法**: `POST`  
**请求路径**: `/api/admin/exams/{id}/publish`

### 2.11 关闭考试
**接口名称**: 关闭考试  
**请求方法**: `POST`  
**请求路径**: `/api/admin/exams/{id}/close`

### 2.12 获取考试题目列表
**接口名称**: 获取考试题目  
**请求方法**: `GET`  
**请求路径**: `/api/admin/exams/{id}/questions`

### 2.13 添加考试题目
**接口名称**: 添加考试题目  
**请求方法**: `POST`  
**请求路径**: `/api/admin/exams/{id}/questions`  
**请求体**:
```json
{
  "questionIds": [1, 2, 3, 4, 5]
}
```

### 2.14 移除考试题目
**接口名称**: 移除考试题目  
**请求方法**: `DELETE`  
**请求路径**: `/api/admin/exams/{id}/questions/{questionId}`

---

## 📊 三、仪表盘统计 API 接口

### 3.1 获取仪表盘统计数据
**接口名称**: 获取仪表盘统计  
**请求方法**: `GET`  
**请求路径**: `/api/admin/dashboard/statistics`  
**响应示例**:
```json
{
  "code": 200,
  "data": {
    "totalUsers": 1250,
    "activeUsers": 892,
    "totalQuestions": 3500,
    "totalExams": 156,
    "todayExams": 12,
    "passRate": 87.5
  }
}
```

---

## 🔐 四、通用响应格式

### 成功响应
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 错误响应
```json
{
  "code": 400,
  "message": "错误信息描述",
  "data": null
}
```

### 常见错误码
- `200`: 成功
- `400`: 请求参数错误
- `401`: 未授权
- `403`: 禁止访问
- `404`: 资源不存在
- `500`: 服务器错误

---

## 📝 五、API 接口总计

| 模块 | 接口数量 |
|------|---------|
| 题库管理 | 13 个 |
| 考试管理 | 14 个 |
| 仪表盘统计 | 1 个 |
| **总计** | **28 个** |

---

## 🚀 六、实现优先级

### 第一阶段（核心功能）
- 题库列表查询、创建、编辑、删除
- 题目列表查询、创建、编辑、删除
- 考试列表查询、创建、编辑、删除
- 考试成绩查询

### 第二阶段（扩展功能）
- 批量导入/导出
- 考试监控
- 统计数据分析

### 第三阶段（高级功能）
- 考试发布/关闭
- 题目随机组卷
- 成绩分析报表

---

**文档版本**: 1.0  
**最后更新**: 2024-03-25  
**维护者**: 前端团队

